# ── Gradle JVM Settings ─────────────────────────────────────────────────
# زيادة الذاكرة لتجنب: "insufficient memory for the Java Runtime Environment"
org.gradle.jvmargs=-Xmx4096m -Xms1024m -XX:MaxMetaspaceSize=1024m -XX:ReservedCodeCacheSize=512m -Dfile.encoding=UTF-8

# ── Daemon — إيقاف لتوفير RAM ───────────────────────────────────────────
# daemon=false يمنع تراكم عمليات Java في الخلفية
org.gradle.daemon=false
org.gradle.parallel=false
org.gradle.caching=false
org.gradle.configureondemand=false

# ── AndroidX ────────────────────────────────────────────────────────────
android.useAndroidX=true
android.enableJetifier=true

# ── Kotlin ──────────────────────────────────────────────────────────────
kotlin.incremental=false
